# datstart

A personal browser start page — clock, weather, search, pinned link cards and an app dock.
Dark neutral liquid-glass UI with a neon green accent.

## Development

```bash
npm install
npm run dev
```

Open http://localhost:3000
